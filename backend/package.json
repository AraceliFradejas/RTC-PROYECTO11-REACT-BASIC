{
  "name": "@archivo-x/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test",
    "catalog:preview": "node --env-file-if-exists=.env scripts/import-episodes.js",
    "catalog:import": "node --env-file-if-exists=.env scripts/import-episodes.js --apply"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.10.1"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
